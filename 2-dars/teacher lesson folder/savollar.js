//  DATATYPES in JS
/**
       * Primitive:  
       * Number, 
       * String, 
       * Boolean, 
       * Symbol, 
       * BigInt, 
       * undefined, 
       * null
       * 

       * Reference: 
      Object (Array, Function)
       *  */

/** let vs var => farqi nimada ?
 * 1. hoisting
 * 2. varda bitta o'zgaruvchi bir necha marta qayta ochilishi mumkin
 * 3. scope
 * 4. version, let -> es6
 *
 * (const, let) - es6
 *
 */

/** null vs undefined (qiymatsiz) => farqi nimada ?
 *
 * 1. conversion to number (raqamga o'zgartirishda)
 *    null + 10 -> son
 *    undefined + 10 -> NaN (Not A Number)
 * ikkalasi ham bo'shliq faqat o'zgaruvchini null
 * orqali bo'shlab qo'yamiz
 *
 */

/** is not defined vs undefined => farqi nimada ?
 *
 *  is not defined -> e'lon qilinmagan o'zgaruvchi
 *  undefined -> o'zgaruvchini qiymati yo'q
 *
 */

/**  const vs let => farqi nimada ?
 *
 * 1. const ni qiymatini o'zgartirib bo'lmaydi
 * 2. const o'zgaruvchiga qiymat berish majburiy
 *
 */

/**  == vs === => farqi nimada ?
 * 
 * == -> faqat qiymatlar tengligini tekshradi
 * === -> qiymat va datatypelar tengligini tekshiradi
 * 
 */






